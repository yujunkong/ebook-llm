# 7강. 파일 입출력과 데이터 다루기
## 이번 강에서 배우는 내용

- open으로 텍스트 파일을 읽고 쓴다
- with 문으로 파일을 안전하게 닫는다
- JSON, CSV 형식을 읽고 쓴다
- pathlib.Path로 경로를 다룬다
- 작은 말뭉치 파일을 읽어 토큰 ID로 변환하는 파이프라인을 만든다

## 왜 중요한가?
LLM은 데이터가 없으면 학습할 수 없다.

실제 파이프라인은 대략 이렇게 시작한다.

```text
디스크의 텍스트/JSON/CSV
  → 읽기
    → 정제
      → 토큰화
        → ID 시퀀스
          → 배치
            → 모델 학습
```

아직 모델은 만들지 않지만, “파일을 열어 줄 단위로 읽고 구조화한다”는 기술은 Pretraining 데이터 준비·SFT 데이터셋·평가 로그 저장까지 계속 쓰인다.

실험 재현성 측면에서도, 설정(JSON)과 메트릭(CSV/JSONL)을 파일로 남기는 습관이 중요하다.

## 선수 개념
### 2.1 File I/O

**File I/O(파일 입출력)**는 프로그램이 디스크의 파일을 읽거나 쓰는 작업이다.

메모리는 프로그램을 끄면 사라질 수 있다. 파일은 그 결과를 **남겨 두는 저장소**이다.

### 2.2 텍스트와 바이너리

초보 단계에서 다루는 대부분은 **텍스트 파일**이다. (`.txt`, `.csv`, `.json`)

나중에 모델 가중치는 바이너리로 저장되지만, 7강의 범위는 텍스트·구조화 텍스트에 둔다.

### 2.3 Encoding

**Encoding(인코딩)**은 문자를 바이트로 표현하는 규칙이다. 한국어·이모지를 다루려면 보통 **UTF-8**을 명시한다.

```python
open("data.txt", "r", encoding="utf-8")
```

## 핵심 개념
### 3.1 파일 쓰기와 읽기

쓰기:

```python
# write_demo.py
# 목적: 텍스트 파일을 생성한다.

from pathlib import Path

def main() -> None:
    path = Path("corpus.txt")
    # "w": 쓰기 모드 (기존 내용 덮어씀)
    with path.open("w", encoding="utf-8") as f:
        f.write("오늘 날씨가 좋다\n")
        f.write("오늘 기분이 좋다\n")
    print("wrote", path)

if __name__ == "__main__":
    main()
```

읽기:

```python
# read_demo.py
from pathlib import Path

def main() -> None:
    path = Path("corpus.txt")
    with path.open("r", encoding="utf-8") as f:
        text = f.read()
    print(text)

if __name__ == "__main__":
    main()
```

### 3.2 with 문

**with**는 컨텍스트가 끝날 때 파일을 자동으로 닫아 준다.

```python
with open("corpus.txt", "r", encoding="utf-8") as f:
    for line in f:
        print(line.strip())
# 이 지점에서 파일이 닫힌다
```

`close()`를 잊으면 파일 핸들이 남는 문제가 생길 수 있다. 습관적으로 `with`를 쓴다.

### 3.3 모드 요약

| 모드 | 의미 |
|---|---|
| `"r"` | 읽기 |
| `"w"` | 쓰기(덮어쓰기) |
| `"a"` | 이어 쓰기(append) |
| `"x"` | 새 파일 전용 생성(이미 있으면 에러) |

텍스트가 기본이다. 바이너리는 `"rb"`, `"wb"`처럼 `b`를 붙인다.

### 3.4 줄 단위 처리

큰 파일은 `read()`로 한 번에 다 읽지 않는 편이 안전하다. 줄 단위로 순회한다.

```python
from pathlib import Path

def iter_nonempty_lines(path: Path):
    with path.open("r", encoding="utf-8") as f:
        for line in f:
            line = line.strip()
            if line:
                yield line
```

`yield`는 **Generator(제너레이터)**를 만드는 문법이다.  
지금은 “한 줄씩 흘려보낸다” 정도로 이해하면 충분하다. 대용량 말뭉치에서 메모리를 아끼는 기본 패턴이다.

줄이 $N$줄이고 줄당 평균 토큰 수가 $\bar{\ell}$이면, 파일 전체를 한 번에 리스트로 모을 때 대략

$$
\text{메모리} \propto N \cdot \bar{\ell}
$$

스케일로 늘어납니다. 제너레이터로 한 줄씩 처리하면 **동시에 들고 있는 양**을 $O(\bar{\ell})$ 근처로 줄일 수 있습니다. (물론 vocab 등 전역 구조는 별도입니다.)

### 3.5 JSON

**JSON(JavaScript Object Notation)**은 딕셔너리·리스트와 잘 맞는 텍스트 데이터 형식이다. 설정 파일과 API 응답에 흔하다.

```python
# json_demo.py
import json
from pathlib import Path

def main() -> None:
    config = {
        "model": "toy-gpt",
        "lr": 3e-4,
        "batch_size": 4,
        "special_tokens": ["<pad>", "<unk>"],
    }
    path = Path("config.json")
    path.write_text(json.dumps(config, ensure_ascii=False, indent=2), encoding="utf-8")

    loaded = json.loads(path.read_text(encoding="utf-8"))
    print(loaded["lr"])

if __name__ == "__main__":
    main()
```

- `json.dumps`: Python 객체 → JSON 문자열
- `json.loads`: JSON 문자열 → Python 객체
- `ensure_ascii=False`: 한글을 `\uXXXX`로 이스케이프하지 않음
- `indent=2`: 사람이 읽기 쉬운 들여쓰기

**JSONL(JSON Lines)**는 한 줄에 JSON 객체 하나인 형식이다. 대용량 데이터셋에 자주 쓰인다.

```text
{"prompt": "안녕", "response": "안녕하세요"}
{"prompt": "날씨?", "response": "맑습니다"}
```

```python
import json
from pathlib import Path

def load_jsonl(path: Path) -> list[dict]:
    rows = []
    with path.open("r", encoding="utf-8") as f:
        for line in f:
            line = line.strip()
            if line:
                rows.append(json.loads(line))
    return rows
```

SFT 데이터셋이 이런 형태인 경우가 많다.

### 3.6 CSV

**CSV(Comma-Separated Values)**는 표 형식 텍스트이다. 실험 로그·메트릭 저장에 자주 쓴다.

```python
# csv_demo.py
import csv
from pathlib import Path

def main() -> None:
    path = Path("metrics.csv")
    rows = [
        {"step": 100, "loss": 2.5},
        {"step": 200, "loss": 1.8},
    ]

    with path.open("w", encoding="utf-8", newline="") as f:
        writer = csv.DictWriter(f, fieldnames=["step", "loss"])
        writer.writeheader()
        writer.writerows(rows)

    with path.open("r", encoding="utf-8", newline="") as f:
        reader = csv.DictReader(f)
        for row in reader:
            # CSV는 문자열로 읽히므로 필요 시 변환
            print(int(row["step"]), float(row["loss"]))

if __name__ == "__main__":
    main()
```

주의: CSV에서 읽은 값은 기본적으로 **문자열**이다. 숫자 계산 전에 `int`/`float` 변환이 필요하다.

### 3.7 pathlib

**pathlib.Path**는 파일 경로를 객체로 다루는 표준 라이브러리이다.

```python
from pathlib import Path

data_dir = Path("data")
data_dir.mkdir(exist_ok=True)
file_path = data_dir / "corpus.txt"  # 운영체제에 맞게 경로 결합
print(file_path.name)     # corpus.txt
print(file_path.suffix)   # .txt
print(file_path.exists())
```

문자열로 `"data" + "/" + "corpus.txt"`를 직접 이어 붙이는 것보다 안전하고 읽기 쉽다.

## 직관적으로 이해하기
파일을 창고, 프로그램을 작업대로 비유하자.

| 작업 | 비유 |
|---|---|
| write | 창고에 상자 보관 |
| read | 창고에서 상자 꺼내기 |
| JSON | 라벨이 잘 붙은 정리된 상자 |
| CSV | 표가 그려진 대장 |
| JSONL | 한 줄에 상자 하나, 컨베이어 벨트 |
| pathlib | 창고 주소 체계 |

LLM 학습은 “창고에서 원문을 꺼내 가공해 모델에 공급”하는 물류에 가깝다.

## 작은 숫자·데이터 예제
`corpus.txt`:

```text
오늘 날씨가 좋다
오늘 기분이 좋다
내일 날씨가 맑다
```

목표:

1. 파일을 줄 단위로 읽는다
2. 6강 방식의 vocab을 만든다
3. 각 줄을 ID 리스트로 인코딩해 `encoded.jsonl`로 저장한다

첫 줄 `"오늘 날씨가 좋다"`가 `[2, 3, 4]`가 된다고 가정하면(특수 토큰 예약 후), 파일로 남기는 결과는 대략:

```text
{"text": "오늘 날씨가 좋다", "ids": [2, 3, 4]}
```

이 한 줄이 “원시 텍스트 → 학습 가능한 숫자 시퀀스”의 기록이다.

### 손계산 — vocab 크기와 인코딩

세 줄 말뭉치에서 고유 토큰이 `{오늘, 날씨가, 좋다, 기분이, 내일, 맑다}` 여섯 개이고 `<pad>`, `<unk>`를 예약하면

$$
|\mathcal{V}| = 2 + 6 = 8
$$

입니다. 첫 줄의 ID 길이는 토큰 수와 같습니다.

$$
\ell(\text{“오늘 날씨가 좋다”}) = 3
$$

JSONL 한 줄은 대략 $(\text{text}, \mathbf{id})$ 쌍을 직렬화한 것입니다.

### 미니 예제 — CSV 평균 Loss

스텝별 loss $L_1,\ldots,L_5 = 3.0, 2.5, 2.1, 1.8, 1.6$의 평균은

$$
\bar{L} = \frac{1}{5}\sum_{t=1}^{5} L_t = \frac{11.0}{5} = 2.2
$$

입니다. CSV에서 읽은 문자열을 `float`로 바꾼 뒤 같은 식으로 계산합니다.

```python
losses = [3.0, 2.5, 2.1, 1.8, 1.6]
print(sum(losses) / len(losses))  # 2.2
```

### 미니 예제 — JSON 설정의 학습률

설정 파일에 `"lr": 3e-4`가 있으면, 로드 후 파이썬 값으로

$$
\eta = 3 \times 10^{-4}
$$

가 됩니다. **파일(문자열/JSON) → float 변수** 변환이 실험 재현의 출발점입니다.

## 코드로 파이프라인 만들기
```python
# prepare_corpus.py
# 목적: 텍스트 파일을 읽어 vocab/인코딩 결과를 저장한다.

from __future__ import annotations

import json
from pathlib import Path

def read_lines(path: Path) -> list[str]:
    lines: list[str] = []
    with path.open("r", encoding="utf-8") as f:
        for line in f:
            line = line.strip()
            if line:
                lines.append(line)
    return lines

def build_vocab(lines: list[str]) -> dict[str, int]:
    vocab = {"<pad>": 0, "<unk>": 1}
    for line in lines:
        for tok in line.split():
            if tok not in vocab:
                vocab[tok] = len(vocab)
    return vocab

def encode(text: str, vocab: dict[str, int]) -> list[int]:
    unk = vocab["<unk>"]
    return [vocab.get(t, unk) for t in text.split()]

def main() -> None:
    data_dir = Path("data")
    data_dir.mkdir(exist_ok=True)

    corpus_path = data_dir / "corpus.txt"
    if not corpus_path.exists():
        corpus_path.write_text(
            "오늘 날씨가 좋다\n오늘 기분이 좋다\n내일 날씨가 맑다\n",
            encoding="utf-8",
        )

    lines = read_lines(corpus_path)
    vocab = build_vocab(lines)

    (data_dir / "vocab.json").write_text(
        json.dumps(vocab, ensure_ascii=False, indent=2),
        encoding="utf-8",
    )

    out_path = data_dir / "encoded.jsonl"
    with out_path.open("w", encoding="utf-8") as f:
        for line in lines:
            row = {"text": line, "ids": encode(line, vocab)}
            f.write(json.dumps(row, ensure_ascii=False) + "\n")

    print(f"lines={len(lines)}, vocab_size={len(vocab)}")
    print(f"wrote {out_path}")

if __name__ == "__main__":
    main()
```

이 스크립트가 하는 일:

```text
corpus.txt → vocab.json + encoded.jsonl
```

이후 강의에서 `encoded.jsonl`의 `ids`를 텐서로 바꾸면 학습 입력이 된다.

## 파일을 행렬·시퀀스로 보기

디스크의 표·로그·말뭉치는 메모리에 올리면 배열이 됩니다. 가장 흔한 표 형태는

$$
X\in\mathbb{R}^{N\times d}
$$

입니다. 행 $i$는 $i$번째 샘플, 열 $j$는 $j$번째 특징입니다.

LLM 학습 데이터는 종종 **토큰 ID 행렬**입니다.

$$
X^{\mathrm{tok}}\in\{0,\ldots,V-1\}^{B\times T}
$$

- $B$: 배치 크기
- $T$: 시퀀스 길이
- 각 원소: vocabulary 안의 정수 ID

7강에서 익히는 `txt` / `json` / `jsonl` 읽기는, 결국 이 행렬을 **만들기 위한 원천**을 다루는 일입니다.

### JSONL 한 줄 = 한 샘플

JSONL 파일의 $i$번째 줄을 객체 $r_i$라 하면

$$
\mathcal{D} = \{r_1, r_2, \ldots, r_N\}
$$

입니다. 예: `{"text": "...", "label": 1}`.

토큰화 후 ID 열로 바꾸면

$$
r_i \mapsto (x_{i,1},\ldots,x_{i,T_i})
$$

길이가 제각각이면 패딩·패킹으로 $T$를 맞춥니다.

### 바이트·문자·토큰

파일 크기를 바이트 $M$이라 할 때, UTF-8 텍스트의 문자 수 $C$와 토큰 수 $T$는 일반적으로

$$
M \ge C,\qquad T \le C
\quad\text{(언어·Tokenizer에 따라 비율이 다름)}
$$

입니다. “GB 말뭉치”를 들으면 대략 **토큰 수 스케일**을 가늠하는 습관이 필요합니다.

$$
\hat{T} \approx \alpha \cdot M
$$

($\alpha$는 바이트당 토큰 비율의 경험적 계수입니다.)

### 스트리밍 합·평균

전체를 RAM에 올리지 않고 줄 단위로 읽을 때, 합과 개수는 온라인으로 갱신합니다.

$$
S \leftarrow S + x_t,\qquad
n \leftarrow n + 1,\qquad
\bar{x} = \frac{S}{n}
$$

```python
# 줄 단위로 손실 로그 평균 내기
path = "losses.txt"  # 한 줄에 float 하나라고 가정
S, n = 0.0, 0
with open(path, encoding="utf-8") as f:
    for line in f:
        line = line.strip()
        if not line:
            continue
        S += float(line)
        n += 1
mean = S / n if n else float("nan")
print(n, mean)
```

### train/val 분할을 수식으로

전체 인덱스 $\{1,\ldots,N\}$을 학습·검증으로 나눕니다.

$$
\mathcal{I}_{\mathrm{train}} \cup \mathcal{I}_{\mathrm{val}} = \{1,\ldots,N\},
\quad
\mathcal{I}_{\mathrm{train}} \cap \mathcal{I}_{\mathrm{val}} = \emptyset
$$

비율 $r\in(0,1)$이면 대략 $|\mathcal{I}_{\mathrm{val}}|\approx rN$입니다.

```python
# 간단한 hold-out (셔플은 생략한 스케치)
N, r = 1000, 0.1
n_val = int(N * r)
idx_val = list(range(n_val))
idx_train = list(range(n_val, N))
```

### 체크포인트도 파일이다

학습 상태 $\theta_t$를 시각 $t$에 저장하면

$$
\mathrm{save}:\ \theta_t \mapsto \texttt{ckpt\_t.pt}
$$

복원은 역방향입니다.

$$
\mathrm{load}:\ \texttt{ckpt\_t.pt} \mapsto \hat\theta_t
$$

지금은 텍스트 JSON만 다뤄도, “상태를 직렬화한다”는 사고는 동일합니다.

### vocab.json의 수학

단어→ID 맵을

$$
\mathrm{vocab}:\ \mathcal{W}\to\{0,\ldots,V-1\}
$$

라 하면, 인코딩은

$$
\mathrm{encode}(w_1,\ldots,w_L)
=
\bigl(\mathrm{vocab}(w_1),\ldots,\mathrm{vocab}(w_L)\bigr)
$$

입니다. 파일로 저장해 두면 **학습과 추론이 같은 맵**을 공유합니다.

> **핵심**
>
> 7강의 파일 I/O는 “디스크 ↔ 파이썬 객체 ↔ (나중) 텐서” 파이프의 첫 밸브입니다.

## LLM에서는 어디에 사용될까?
| 형식 | LLM 워크플로에서의 예 |
|---|---|
| `.txt` / `.jsonl` | Pretraining 말뭉치, 문서 스트림 |
| JSON | 학습 설정, Tokenizer 설정, 모델 config |
| JSONL | SFT(instruction-response) 데이터 |
| CSV | step별 loss, 평가 점수 로그 |
| pathlib | 체크포인트 디렉터리, 데이터셋 경로 관리 |

대규모 데이터는 한 번에 RAM에 올리지 않는다.

```text
파일 스트림 → 미니배치 → GPU
```

7강에서 줄 단위 읽기·JSONL을 익히는 이유는, 나중에 `Dataset`/`DataLoader`를 만날 때 “데이터가 원래 어디에 있었는가”를 이해하기 위해서다.

체크포인트도 결국 파일이다.

```text
model.pt / model.safetensors
optimizer 상태
trainer_state.json
```

지금은 텍스트만 다루지만, “상태를 파일로 남긴다”는 사고방식은 동일하다.

## 실습
### 실습 1 — 일기 파일

`notes.txt`에 세 줄을 쓰고, 다시 읽어 줄 수를 출력한다.

### 실습 2 — 설정 JSON

`train_config.json`에 `lr`, `epochs`, `model_name`을 저장하고 읽어 출력한다.

### 실습 3 — 메트릭 CSV

step=1..5, loss가 `3.0, 2.5, 2.1, 1.8, 1.6`인 CSV를 만든 뒤, 평균 loss를 계산한다.

### 실습 4 — JSONL 변환

실습 1의 각 줄을 `{"text": ...}` JSONL로 저장한다.

## 자주 하는 실수
1. **인코딩을 생략해 한글이 깨진다**  
   `encoding="utf-8"`을 명시한다.

2. **`with` 없이 열고 닫기를 잊는다**  
   특히 예외가 나면 파일이 열린 채로 남을 수 있다.

3. **`"w"`로 열어 기존 데이터를 날린다**  
   이어 쓰려면 `"a"`, 새 파일만 만들려면 `"x"`를 검토한다.

4. **CSV 숫자를 문자열 그대로 평균 낸다**  
   반드시 `float` 변환 후 계산한다.

5. **경로를 문자열로 난잡하게 이어 붙인다**  
   `Path("a") / "b" / "c.txt"`를 사용한다.

6. **큰 파일을 `read()`로 한 번에 읽는다**  
   말뭉치가 커지면 메모리 부족이 난다. 줄/청크 단위로 처리한다.

## 핵심 요약
- 파일 입출력은 데이터를 프로그램 밖으로 저장·재유입하는 기술이다
- `with` + `encoding="utf-8"`이 텍스트 처리의 기본이다
- JSON/JSONL은 중첩 구조·샘플 스트림에, CSV는 표형 로그에 적합하다
- pathlib로 경로를 안전하게 다룬다
- “텍스트 파일 → vocab/ids 파일” 파이프라인이 LLM 데이터 준비의 축소판이다
- 줄 단위 스트림은 메모리 $O(N\cdot\bar{\ell})$ 부담을 줄이는 기본 전략이다
- CSV 메트릭의 평균 $\bar{L}=\frac{1}{n}\sum L_t$는 float 변환 뒤에 계산한다

## 용어 사전
| 용어 | 의미 |
|---|---|
| File I/O | 파일 읽기/쓰기 |
| Encoding / UTF-8 | 문자를 바이트로 표현하는 규칙 |
| with | 리소스를 안전하게 여닫는 문법 |
| JSON | 딕셔너리·리스트형 텍스트 데이터 형식 |
| JSONL | 한 줄 하나의 JSON 객체 |
| CSV | 쉼표(또는 구분자)로 구분된 표 형식 |
| pathlib.Path | 객체형 경로 다루기 |
| Generator (`yield`) | 값을 한 번에 하나씩 생산하는 방식 |
| Vocabulary size $\|\mathcal{V}\|$ | 특수 토큰 포함 어휘 개수 |
| Corpus | 학습에 쓰는 원문 말뭉치 |

## 연습문제
### 문제 1 (개념)

`"w"` 모드와 `"a"` 모드의 차이를 설명하시오.

### 문제 2 (코드)

`Path("data") / "a.json"`이 문자열 이어붙이기보다 나은 이유를 한 문장으로 쓰시오.

### 문제 3 (JSON)

다음 Python 객체를 JSON 문자열로 바꾸는 함수 이름을 쓰시오.

```python
{"lr": 0.001, "epochs": 3}
```

### 문제 4 (응용)

JSONL 파일에서 `response` 필드만 리스트로 모으는 코드를 스케치하시오.

### 문제 5 (연결)

SFT 데이터셋을 JSONL로 두는 이유가 Pretraining용 거대 `.txt` 스트림과 어떻게 다른 요구에서 비롯되는지 설명하시오.

### 문제 6 (수식)

loss가 $1.0, 2.0, 3.0$일 때 $\bar{L}$은?

### 문제 7 (어휘)

특수 토큰 2개와 고유 단어 10개로 vocab을 만들면 $|\mathcal{V}|$는?

---

## 정답 및 해설
### 문제 1

`"w"`는 파일을 새로 쓰며 기존 내용을 덮어쓴다. `"a"`는 파일 끝에 내용을 이어 붙인다.

### 문제 2

운영체제별 경로 구분자를 Path가 처리해 주어 이식성과 가독성이 좋다.

### 문제 3

`json.dumps`

### 문제 4 예시

```python
responses = []
with Path("sft.jsonl").open("r", encoding="utf-8") as f:
    for line in f:
        responses.append(json.loads(line)["response"])
```

### 문제 5

SFT는 보통 (지시, 응답)처럼 필드가 있는 샘플 단위가 필요하다. JSONL은 샘플마다 구조를 보존하기 쉽다. Pretraining은 대량 원문을 흘려보내면 되어 순수 텍스트 스트림도 흔하다.

### 문제 6

$\bar{L}=2.0$

### 문제 7

$|\mathcal{V}|=12$

## 다음 강의와 연결
이전 **제6강. 리스트, 튜플, 딕셔너리**에서 메모리 속 구조를 익혔고, 이번 강의에서 파일과 연결했다.

다음 **제8강. NumPy로 배열 다루기**에서는 ID 리스트를 넘어, 수치 계산에 특화된 배열을 배운다.  
shape·dtype·벡터화 연산은 이후 Tensor와 PyTorch의 직접적인 다리이다.

> 데이터를 불러올 수 있게 되었다. 이제 숫자를 빠르게 계산하는 배열로 옮긴다.

<!-- LECTURE_NAV -->

---

### 강의 이동

- **이전 강:** [6강. 리스트, 튜플, 딕셔너리](06강_리스트_튜플_딕셔너리.md)
- **다음 강:** [8강. NumPy로 배열 다루기](08강_NumPy로_배열_다루기.md)

<!-- /LECTURE_NAV -->
